# 桌面小秘书 DeskSecretary — 功能介绍

> 一款 AI 驱动的桌面效率伴侣，待办管理 × 健康提醒 × 智能复盘，贴在屏幕边缘随时待命。

---

## 🛠️ 核心功能

### 1. 智能待办管理
- **自然语言建任务** — 输入「明天下午 3 点和产品对需求」，AI 自动解析出标题、优先级、截止时间
- **优先级小灯泡** — 红灯 = 高优 / 绿灯 = 普通，点击即可切换，7 层玻璃球渐变 + 灯丝发光效果
- **任务笔记** — 每条任务支持添加文字笔记、图片、链接、文档附件
- **今日复盘** — 一键生成当日工作日报（完成 / 未完成 / 小结），自动保存可回溯

### 2. 定时休息提醒
- **智能倒计时** — 自定义间隔，久坐自动弹出提醒
- **AI 温暖文案** — 每次提醒不是冷冰冰的弹窗，而是 AI 生成的关怀文案（"站起来伸个懒腰吧 🌿"）
- **Snooze 支持** — 2 分钟 / 5 分钟 / 知道了，灵活延迟
- **30 秒自动关闭** — 不打扰你，静默消失

### 3. AI 智能复盘
- **今日日报** — 自动汇总完成 / 未完成任务，配合笔记和图片描述，一键生成结构化日报
- **图片 Vision 识别** — 日报中可附带截图，AI 自动识别图片内容写入小结
- **周报整理** — 按周自动汇总任务完成率、优先级分布、工作小结，支持前后翻周
- **自动生成** — 每日 22:00 自动生成日报，每周日自动生成周报，保存到本地可随时查看

### 4. 边缘悬浮 · 迷你模式
- **屏幕边缘吸附** — 拖到屏幕边缘自动折叠成 48px 超薄迷你条，贴边不占空间
- **表情动画** — 迷你条上有眼睛 + 嘴巴动画，6 种表情自动轮播（微笑/吃惊/悲伤/惊讶/平静/眨眼），平滑过渡
- **6 套配色** — Aurora / Ocean / Forest / Twilight / Morandi / Purple，设置里一键切换
- **悬浮展开** — 鼠标移入自动展开完整窗口，移出自动折叠

### 5. 全局热键
- **一键呼出** — 默认 `Alt + Space`，无论你在哪个软件里都能瞬间唤出小秘书
- **Windows 原生实现** — RegisterHotKey + NativeEventFilter，稳定无冲突

### 6. 跨设备同步
- **GitHub 私有仓库** — 数据自动加密推送，启动拉取 + 定时推送 + 退出推送
- **手机 PWA 伴侣** — iPhone Safari 添加到主屏即可使用，查看今日任务 + 快速添加
- **双向数据流** — 电脑端任务同步到手机，手机端新增任务自动回到电脑

---

## ✨ 产品亮点

| 亮点 | 说明 |
|------|------|
| **零感知 AI** | AI 调用全在子线程，UI 零卡顿；API 不可用时自动降级为本地方案，不影响使用 |
| **灯泡指示器** | 不是色块圆点，是 7 层渲染的迷你灯泡——玻璃球体 + 灯丝发光 + 鼠标悬浮放大 |
| **表情迷你条** | 屏幕边缘的会动的小脸，6 种表情平滑切换，工作间隙的小治愈 |
| **AI 文案提醒** | 每次休息提醒文案不同，像同事在旁边关心你而不是闹钟在催你 |
| **一键复盘** | 不用自己写日报——AI 自动整理完成/未完成/小结，图片和笔记自动纳入 |
| **全量 Token 管理** | AI 用量按类型统计 + 周限额保护，用多少心里有数 |
| **深浅双主题** | 奶油白 + 深紫夜，一键切换，所有组件联动适配 |
| **离线也能用** | SQLite 本地存储，断网不影响日常使用，AI 降级为本地方案 |

---

## 🏗️ 技术架构

```
┌─────────────────────────────────────────┐
│              UI 层 (PyQt6)              │
│  FloatingWindow / MiniBar / TaskList    │
│  ReviewDialog / SettingsDialog / ...    │
├─────────────────────────────────────────┤
│            Service 层 (QThread)         │
│  AIService / ReminderService            │
│  HotkeyService / SyncService            │
│  AutoReportService                      │
├─────────────────────────────────────────┤
│             Data 层 (Repository)        │
│  TaskRepo / SettingsRepo / ReportRepo   │
│  TokenRepo                              │
├─────────────────────────────────────────┤
│          DB 层 (SQLite + WAL)           │
│  tasks / settings / task_notes          │
│  reports / token_usage / reminder_hist  │
└─────────────────────────────────────────┘
```

- **语言**：Python 3.12 + PyQt6
- **AI 接口**：OpenAI 兼容 API（支持 DeepSeek / GPT / 通义千问等）
- **数据存储**：SQLite（WAL 模式）+ GitHub 私有仓库云同步
- **移动端**：PWA（HTML5 + Service Worker），通过 GitHub Contents API 交换数据
- **热键**：Windows 原生 RegisterHotKey，主线程无 COM 冲突

---

## 📱 使用方式

1. **启动** — 双击 `启动小秘书.bat` 或 `py main.py`
2. **添加任务** — 在输入框输入自然语言，回车即添加
3. **切换优先级** — 点击任务前的灯泡，红/绿切换
4. **休息提醒** — 默认每 45 分钟提醒一次，在设置中调整
5. **生成日报** — 点击 📊 → 今日复盘，AI 自动生成
6. **迷你模式** — 把窗口拖到屏幕边缘，自动折叠成迷你条
7. **手机查看** — 部署 PWA 后 iPhone 添加到主屏即可

---

## 🎯 适合谁

- 🖥️ 长时间伏案、需要定时休息提醒的打工人
- 📋 每天要写日报/周报、想节省整理时间的开发者
- 🧠 习惯用自然语言记事、不想手动填优先级和截止时间的效率党
- 🎨 喜欢桌面小工具、追求精致 UI 的审美派

---

*DeskSecretary — 贴在屏幕边缘的 AI 效率搭档 🛠️*
